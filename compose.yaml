services:
  web:
    build: .
    ports:
      - "${APP_PORT}:5000"
    environment:
      # - SECRET_KEY=$
      # - DEBUG
      # - ALLOWED_HOSTS
      # - DATABASE_URL

  # redis:
  #   image: redis:alpine
