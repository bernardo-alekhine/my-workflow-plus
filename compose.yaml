services:
  django_web:
    image: my-workflow-plus:v0.1.0
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "${APP_PORT}:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
    env_file:
      - .env
